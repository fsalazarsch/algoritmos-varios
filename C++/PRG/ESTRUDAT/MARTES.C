/* crear una lista de modo que contenga tres caracteres A, B, C.*/
#include<stdio.h>
#include<conio.h>
#include<alloc.h>
#define
main()
{
clrscr();
struct nodo *cab,*q;
cab=memoria;
cab->info=a;
q->memoria;
q->info=b;
cab->sig=q; // enlaza el nodo
q->sig=memoria;
q->sig->info=c;
q->sig->=NULL;
}
getch();
}
cuando a cualquier apuntador se le asigna null
es fin de la lista o que la lista esta basia.
//ej:crear una lista que reciba cinco enteros susesivos desde 1 hasta5
1 estado.
cab = null;
valor=5;
q=memoria;
q->info=valor;
q->sig=cab;
cab =q;

2 estado
valor--;
q=memoria;
q->info=valor;
q->sig=cab
cab=q;
3 estado
main();
{
clrscr();
/*crear una lista con
/*cinco nodos*/
valor = 5;
cab=null
  while(valor>0)
	{
	  q=memo;
	  q->info=valor;
	  q->sig=cab;
	  cab=q;
	  valor--;
	 }
	  printf("%4d",q->info);
getch();
}